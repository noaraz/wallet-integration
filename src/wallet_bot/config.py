"""Application settings.

Phase 1 replaces this stub with a pydantic-settings-based loader that
reads from environment variables (mapped from Google Secret Manager in prod).
Until then, this module is a placeholder to keep the import graph stable.
"""

from __future__ import annotations
